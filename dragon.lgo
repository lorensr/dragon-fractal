(setpencolor 99 99 0)

to dragon :len :angle :limit
  ifelse (:len < :limit) (draw :len :angle) (recur :len :angle :limit)
end

to recur :len :angle :limit
  if (:len < :limit) stop
  dragon (:len / 2) (:angle + 45) :limit
  dragon (:len / 2) (:angle - 45) :limit
end

to draw :len :angle
  pd
  rt :angle
  fd :len
  pu
end	

pd
to test
  ifelse (2 > 1) (fd 50) (rt 90)
end
